extends ../navigation/layout
include ../mixins/_articleCard


block content
	br
	ul.tags
		each t in tags
			a.tag(href=`/articles/${t._id}` class=(t._id === tag ? 'active' : ''))
				span=t._id
				span=t.count	
	each article in articles
		+articleCard(article)